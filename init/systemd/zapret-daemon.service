[Unit]
Description=Zapret Daemon Service
Documentation=https://github.com/Sergeydigl3/zapret-discord-youtube-ng
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
ExecStart=/usr/bin/zapret-daemon serve --config /etc/zapret/config.yaml
Restart=on-failure
RestartSec=5s

# Runtime directory
RuntimeDirectory=zapret
RuntimeDirectoryMode=0755

# Security settings
NoNewPrivileges=true
PrivateTmp=true
ProtectSystem=strict
ProtectHome=true

# Capabilities needed for iptables manipulation
AmbientCapabilities=CAP_NET_ADMIN CAP_NET_RAW
CapabilityBoundingSet=CAP_NET_ADMIN CAP_NET_RAW

# User and group
User=root
Group=root

# Logging
StandardOutput=journal
StandardError=journal
SyslogIdentifier=zapret-daemon

# Resource limits
LimitNOFILE=65536

[Install]
WantedBy=multi-user.target
